apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "steam-and-powerpipe.fullname" . }}-test-connection"
  labels:
    {{- include "steam-and-powerpipe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "steam-and-powerpipe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
